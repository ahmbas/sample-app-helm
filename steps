#helm create demo
helm install demo demo/
helm upgrade demo demo/ --set image.repository=quay.io/redhattraining/hello-world-nginx --set image.tag=latest
# add config map

helm history demo
helm rollback demo <version>

helm repo add stable https://charts.helm.sh/stable
helm repo search

# custom repo
